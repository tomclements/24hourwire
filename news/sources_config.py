# News sources configuration - re-exports from news.languages registry
# Import from news.languages directly for new code

from news.languages import (
    LANGUAGE_MODULES,
    SUPPORTED_LANGUAGES,
    LANGUAGE_FEEDS,
    LANGUAGE_SOURCE_INFO,
    DEFAULT_SOURCES,
    SOURCES,
    CATEGORY_KEYWORDS,
    LANGUAGE_STOP_WORDS,
    SOURCE_ATTRIBUTION,
    GENERIC_TEXT,
    CATEGORY_NAMES,
    UI_STRINGS,
    LANGUAGE_NAMES,
    PAYWALLED_SOURCES,
)
